{
  "name": "@przemyslawwalczak/nestjs-mongoose-migrations",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rollup": "^3.2.2"
  }
}
